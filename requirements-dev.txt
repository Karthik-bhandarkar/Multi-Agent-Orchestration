# --- Dev / QA / CI (NOT used in production Docker image) ---
# Install with: pip install -r requirements-dev.txt

# Full ML stack for local testing
torch>=2.2.2
transformers>=4.44.2

# UI (local only)
streamlit>=1.36.0

# Testing & linting
pytest>=8.2.2
pytest-asyncio>=0.23.7
flake8>=7.1.0
black>=24.4.2
