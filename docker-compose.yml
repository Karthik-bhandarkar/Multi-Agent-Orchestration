version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: edupulse-backend:latest
    container_name: edupulse-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
